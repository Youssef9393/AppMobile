const mongoose=require("mongoose");

const GroupDetailsSchema= new mongoose.Schema({
       groupName:{ type:String, unique: true},
       firstName:String,
       lastName:String,
       email:String,
       montant:Number,
       telephone:String
},{
    collection:"GroupInfo"
});
mongoose.model("GroupInfo",GroupDetailsSchema);